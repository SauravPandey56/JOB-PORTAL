# Job Portal Frontend (React + Tailwind + Vite)

## Start (local)
1. `cd frontend`
2. `npm install`
3. Copy `.env.example` to `.env`
4. `npm run dev`

The dev server runs on `http://localhost:5173` (or the next free port).

